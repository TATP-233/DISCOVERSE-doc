{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Docusaurus Build and Serve",
      "command": "npm run build && npm run serve",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
